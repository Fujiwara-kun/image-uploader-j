<h1>画像アップローダー</h1>

<%= form_with model: @image, local: true do |form| %>
  <div>
    <%= form.label :file, "画像ファイルを選択" %><br>
    <%= form.file_field :file %>
  </div>

  <%= form.submit "アップロード" %>
<% end %>